# Use npm install (not npm ci) in the Render build command so that
# platform-specific native bindings (rolldown, etc.) are resolved for
# the deploy target OS rather than being locked to the macOS lockfile.
legacy-peer-deps=true
